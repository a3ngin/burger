<h1>Burgers!</h1>

<h2>Burgers that have been tried!</h2>

<ul>
  {{#each burgers}}
    {{#unless this.triedYet}}
<button type="submit" class="subby" data-id"{{id}}" data-subby="{{triedYet}}">{{this.name}}</button>
    {{/unless}}
  {{/each}}

</ul>

<h2>Burgers!</h2>
<form class="create-form" action="/api/burgers" method="POST">
<ul>
  {{#each burgers}}
    {{#if triedYet}}
      {{> burgers/burger-block triedYet=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Burgers Eaten!</h2>

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="triedYet">Tried this one yet?</label><br>
    <input type="radio" name="triedYet" value="1" checked> Tried!<br>
    <input type="radio" name="triedYet" value="0"> Awake
  </div>

  <button type="submit">Add Burger!</button>
</form>
